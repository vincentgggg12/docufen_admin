# Instructions

Firstly move into the locales directory:  

```cd docufen_server/locales/```

Then run the sorting 

```node sort-translations.js```

this will resort all the translation files so that they can be more safely merged in git.